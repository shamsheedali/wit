{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "ignoreCommand": "git branch --show-current | grep -v '^main$'",
  "env": {
    "BUILD_SKIP": "@branch != main"
  }
}
